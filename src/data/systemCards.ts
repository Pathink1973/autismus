import { PictureCard } from '../types';

// Define system cards that will always be available
export const systemCards: PictureCard[] = [
  // Add your system cards here, excluding the "Brincar" card
];